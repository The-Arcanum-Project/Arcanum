**Example Output:**

### Nested Snippets and Markdown
Snippets are injected *before* the page is rendered. This means snippets can safely contain bold text, links, or even entire alert blocks, and they will render perfectly.

* **Syntax:** `{{snippet:contact_support}}`
