<UserControl x:Class = "Arcanum.UI.Components.UserControls.BaseControls.ToggleIcon"
             xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d = "http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable = "d" x:Name = "Root"
             Width = "16" Height = "16" Foreground = "{StaticResource DefaultForeColorBrush}"
             Background = "Transparent">

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source = "/Arcanum_UI;component/Assets/ArcanumShared/DefaultPalette.xaml" />
                <ResourceDictionary Source = "/Arcanum_UI;component/Assets/ArcanumShared/DefaultGeometry.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border BorderBrush = "{StaticResource DefaultBorderColorBrush}"
            BorderThickness = "{Binding BorderThickness, ElementName=Root}" CornerRadius = "3"
            SnapsToDevicePixels = "True" VerticalAlignment = "Center" HorizontalAlignment = "Center">

        <Path Stretch = "Uniform" SnapsToDevicePixels = "True" HorizontalAlignment = "Center"
              VerticalAlignment = "Center" Stroke = "{Binding Foreground, ElementName=Root}" StrokeThickness = "1.5"
              StrokeStartLineCap = "Round" StrokeEndLineCap = "Round" StrokeLineJoin = "Round">

            <Path.Style>
                <Style TargetType = "Path">
                    <Setter Property = "Data" Value = "{Binding UncheckedIcon, ElementName=Root}" />
                    <Style.Triggers>
                        <DataTrigger Binding = "{Binding IsChecked, ElementName=Root}" Value = "True">
                            <Setter Property = "Data" Value = "{Binding CheckedIcon, ElementName=Root}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Path.Style>
        </Path>
    </Border>
</UserControl>