<UserControl x:Class = "Arcanum.UI.Components.UserControls.BaseControls.MinMaxSlider"
             xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters = "clr-namespace:Arcanum.UI.Components.Converters" mc:Ignorable = "d"
             d:DesignHeight = "300" d:DesignWidth = "300">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source = "/Arcanum_UI;component/Components/Styles/Base/BaseTextBoxStyle.xaml" />
                <ResourceDictionary Source = "/Arcanum_UI;component/Components/Styles/Base/BaseButton.xaml" />
                <ResourceDictionary Source = "/Arcanum_UI;component/Components/Styles/Base/BlueTollgeButtonStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key = "SliderRepeatButtonStyle" TargetType = "RepeatButton">
                <Setter Property = "OverridesDefaultStyle" Value = "true" />
                <Setter Property = "IsTabStop" Value = "false" />
                <Setter Property = "Focusable" Value = "false" />
                <Setter Property = "Template">
                    <Setter.Value>
                        <ControlTemplate TargetType = "RepeatButton">
                            <Border Background = "{TemplateBinding Background}" Height = "4" CornerRadius = "2" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>


            <Style x:Key = "SlimSliderThumbStyle" TargetType = "Thumb">
                <Setter Property = "OverridesDefaultStyle" Value = "true" />
                <Setter Property = "Cursor" Value = "Hand" />
                <Setter Property = "Template">
                    <Setter.Value>
                        <ControlTemplate TargetType = "Thumb">
                            <Grid Background = "Transparent">
                                <!-- 
                                   We make this wider than the visual thumb (e.g. 24px).
                                   We use Negative Margins to center it over the 6px root.
                                   Background="Transparent" captures the mouse clicks.
                                -->
                                <Border Width = "24" Height = "24" Margin = "-9,0,-9,0" Background = "Transparent" />

                                <!-- THE VISUAL HANDLE -->
                                <Border x:Name = "VisualThumb" Background = "#E0E0E0" BorderBrush = "#111"
                                        BorderThickness = "1" CornerRadius = "3" Width = "6" Height = "14"
                                        VerticalAlignment = "Center" HorizontalAlignment = "Center" />
                            </Grid>

                            <ControlTemplate.Triggers>
                                <Trigger Property = "IsMouseOver" Value = "True">
                                    <Setter TargetName = "VisualThumb" Property = "Background" Value = "White" />
                                </Trigger>
                                <Trigger Property = "IsDragging" Value = "True">
                                    <Setter TargetName = "VisualThumb" Property = "Background" Value = "White" />
                                    <Setter TargetName = "VisualThumb" Property = "BorderBrush" Value = "#666" />
                                </Trigger>
                                <Trigger Property = "IsEnabled" Value = "False">
                                    <Setter TargetName = "VisualThumb" Property = "Background" Value = "#555" />
                                    <Setter TargetName = "VisualThumb" Property = "BorderBrush" Value = "#333" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>


            <Style x:Key = "SlimColorSliderStyle" TargetType = "Slider">
                <Setter Property = "OverridesDefaultStyle" Value = "true" />
                <Setter Property = "Focusable" Value = "False" /> <!-- Prevents dotted focus rect -->
                <Setter Property = "Template">
                    <Setter.Value>
                        <ControlTemplate TargetType = "Slider">
                            <Grid>
                                <!-- Background Rail (Dark Grey) -->
                                <Border x:Name = "TrackBackground" Height = "4" CornerRadius = "2"
                                        VerticalAlignment = "Center" />

                                <!-- The Track logic -->
                                <Track x:Name = "PART_Track">

                                    <!-- Left Side (Filled) -->
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Style = "{StaticResource SliderRepeatButtonStyle}"
                                                      Background = "{TemplateBinding Foreground}" />
                                    </Track.DecreaseRepeatButton>

                                    <!-- The Thumb -->
                                    <Track.Thumb>
                                        <Thumb Style = "{StaticResource SlimSliderThumbStyle}" />
                                    </Track.Thumb>

                                    <!-- Right Side (Unfilled) -->
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Style = "{StaticResource SliderRepeatButtonStyle}"
                                                      Background = "Transparent" />
                                    </Track.IncreaseRepeatButton>
                                </Track>
                            </Grid>

                            <ControlTemplate.Triggers>
                                <!-- Dim the filled track when disabled/locked -->
                                <Trigger Property = "IsEnabled" Value = "False">
                                    <Setter Property = "Opacity" Value = "0.5" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Converters -->
            <converters:RangeToMarginConverter x:Key = "RangeToMarginConverter" />
        </ResourceDictionary>
    </UserControl.Resources>


    <Grid VerticalAlignment = "Center" Height = "24">
        <!-- 1. The Track Background (Empty part) -->
        <Border Background = "#333" Height = "4" CornerRadius = "2" VerticalAlignment = "Center" Margin = "10,0" />

        <!-- 2. The Range Highlight (The Yellow Bar) -->
        <!-- We calculate the margin and width based on the Min/Max values -->
        <Border Height = "4"
                Background = "{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                CornerRadius = "2" VerticalAlignment = "Center" HorizontalAlignment = "Stretch">
            <Border.Margin>
                <MultiBinding Converter = "{StaticResource RangeToMarginConverter}">
                    <Binding Path = "MinValue" RelativeSource = "{RelativeSource AncestorType=UserControl}" />
                    <Binding Path = "MaxValue" RelativeSource = "{RelativeSource AncestorType=UserControl}" />
                    <Binding Path = "Minimum" RelativeSource = "{RelativeSource AncestorType=UserControl}" />
                    <Binding Path = "Maximum" RelativeSource = "{RelativeSource AncestorType=UserControl}" />
                    <Binding Path = "ActualWidth" RelativeSource = "{RelativeSource AncestorType=Grid}" />
                </MultiBinding>
            </Border.Margin>
        </Border>

        <!-- 3. The Sliders -->
        <!-- Note: We set Foreground to Transparent so they don't draw their own yellow bars -->
        <Slider x:Name = "MinSlider" Margin = "7,0"
                Minimum = "{Binding Minimum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Maximum = "{Binding Maximum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Value = "{Binding MinValue, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                Style = "{StaticResource SlimColorSliderStyle}" Foreground = "Transparent" />

        <Slider x:Name = "MaxSlider" Margin = "7,0"
                Minimum = "{Binding Minimum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Maximum = "{Binding Maximum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Value = "{Binding MaxValue, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                Style = "{StaticResource SlimColorSliderStyle}" Foreground = "Transparent" />
    </Grid>
</UserControl>