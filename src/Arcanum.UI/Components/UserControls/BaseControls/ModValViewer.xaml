<UserControl x:Class = "Arcanum.UI.Components.UserControls.BaseControls.ModValViewer"
             xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
             DataContext = "{Binding RelativeSource={RelativeSource Self}, Path=ViewModel}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source = "../../../Assets/ArcanumShared/DefaultFonts.xaml" />
                <ResourceDictionary Source = "../../../Assets/ArcanumShared/DefaultPalette.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
        <!-- <DataTemplate x:Key = "IntegerEditorTemplate" -->
        <!--               DataType = "{x:Type modifierSystem:ModValInstance}"> -->
        <!--     <baseControls:BaseNumericUpDown Value = "{Binding Value, UpdateSourceTrigger=PropertyChanged}" -->
        <!--                                     VerticalAlignment = "Center" /> -->
        <!-- </DataTemplate> -->
        <!-- -->
        <!-- <DataTemplate x:Key = "FloatEditorTemplate" -->
        <!--               DataType = "{x:Type modifierSystem:ModValInstance}"> -->
        <!--     <baseControls:FloatNumericUpDown Value = "{Binding Value, UpdateSourceTrigger=PropertyChanged}" -->
        <!--                                      VerticalAlignment = "Center" /> -->
        <!-- </DataTemplate> -->
        <!-- -->
        <!-- <DataTemplate x:Key = "BooleanEditorTemplate" -->
        <!--               DataType = "{x:Type modifierSystem:ModValInstance}"> -->
        <!--     <CheckBox IsChecked = "{Binding Value}" -->
        <!--               VerticalAlignment = "Center" -->
        <!--               HorizontalAlignment = "Left" /> -->
        <!-- </DataTemplate> -->
        <!-- -->
        <!-- <DataTemplate x:Key = "StringEditorTemplate" -->
        <!--               DataType = "{x:Type modifierSystem:ModValInstance}"> -->
        <!--     <styleClasses:CorneredTextBox Text = "{Binding Value, UpdateSourceTrigger=PropertyChanged}" -->
        <!--                                   VerticalAlignment = "Center" /> -->
        <!-- </DataTemplate> -->
        <!-- -->
        <!-- <helpers:ValueEditorTemplateSelector x:Key = "ValueEditorSelector" -->
        <!--                                      IntegerTemplate = "{StaticResource IntegerEditorTemplate}" -->
        <!--                                      FloatTemplate = "{StaticResource FloatEditorTemplate}" -->
        <!--                                      BooleanTemplate = "{StaticResource BooleanEditorTemplate}" -->
        <!--                                      DefaultTemplate = "{StaticResource IntegerEditorTemplate}" /> -->

    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width = "5*" />
            <ColumnDefinition Width = "4" />
            <ColumnDefinition Width = "2*" />
            <ColumnDefinition Width = "4" />
            <ColumnDefinition Width = "*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text = "{Binding Definition.UniqueId}"
                   VerticalAlignment = "Center"
                   Margin = "5,0"
                   FontWeight = "SemiBold" />

        <Rectangle Grid.Column = "1"
                   Width = "1"
                   Fill = "{DynamicResource DefaultForeColorBrush}"
                   Margin = "0,2"
                   HorizontalAlignment = "Center"
                   VerticalAlignment = "Stretch" />

        <TextBlock Grid.Column = "2"
                   Text = "{Binding Type}"
                   VerticalAlignment = "Center"
                   HorizontalAlignment = "Center"
                   Margin = "5,0"
                   FontWeight = "SemiBold" />

        <Rectangle Grid.Column = "3"
                   Width = "1"
                   Fill = "{DynamicResource DefaultForeColorBrush}"
                   Margin = "0,2"
                   HorizontalAlignment = "Center"
                   VerticalAlignment = "Stretch" />

        <TextBlock Grid.Column = "4"
                   Text = "{Binding Value}"
                   VerticalAlignment = "Center"
                   HorizontalAlignment = "Right"
                   Margin = "5,0"
                   FontWeight = "SemiBold"
                   FontFamily = "{StaticResource DefaultMonospacedFont}" />

        <!-- <Rectangle Grid.Column="0" -->
        <!--            Grid.ColumnSpan="5" -->
        <!--            Height="1" -->
        <!--            Fill="Transparent" -->
        <!--            Stroke="{StaticResource SelectedBackColorBrush}" -->
        <!--            StrokeThickness="1" -->
        <!--            StrokeDashArray="4, 6" -->
        <!--            StrokeDashCap="Flat" -->
        <!--            HorizontalAlignment="Stretch" -->
        <!--            VerticalAlignment="Stretch" -->
        <!--            SnapsToDevicePixels="True" -->
        <!--            Margin="5, 19, 5, 3"></Rectangle> -->

        <!-- <ContentPresenter Grid.Column="2" -->
        <!--                   Content="{Binding}" -->
        <!--                   ContentTemplateSelector="{StaticResource ValueEditorSelector}" -->
        <!--                   Margin="5,2"/> -->

    </Grid>
</UserControl>