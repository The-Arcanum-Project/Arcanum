<styleClasses:BaseWindow x:Class = "Arcanum.UI.Components.Windows.DebugWindows.DocuPageTest"
                         xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
                         xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
                         xmlns:md = "clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
                         WindowStartupLocation = "CenterScreen"
                         xmlns:styleClasses = "clr-namespace:Arcanum.UI.Components.StyleClasses" mc:Ignorable = "d"
                         Title = "DocuPageTest" Height = "800" Width = "1200">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width = "200" />
            <ColumnDefinition Width = "*" />
        </Grid.ColumnDefinitions>

        <!-- ListView for documentation pages -->
        <ListView Grid.Column = "0" Margin = "5"
                  ItemsSource = "{Binding DocuPages, RelativeSource={RelativeSource AncestorType=Window}}"
                  SelectionChanged = "DocuPageListView_SelectionChanged">
        </ListView>

        <!-- Content area for selected documentation page -->
        <md:MarkdownViewer Grid.Column = "1" x:Name = "Viewer" Margin = "0" TextOptions.TextFormattingMode = "Display"
                           TextOptions.TextRenderingMode = "ClearType" ScrollViewer.IsDeferredScrollingEnabled = "True">
            <FrameworkElement.CommandBindings>
                <CommandBinding Command = "{x:Static md:Commands.Hyperlink}" Executed = "OpenHyperlink" />
                <!-- <CommandBinding Command="{x:Static md:Commands.Image}" Executed="ClickOnImage" /> -->
            </FrameworkElement.CommandBindings>
        </md:MarkdownViewer>
    </Grid>
</styleClasses:BaseWindow>