<styleClasses:BaseWindow x:Class = "Arcanum.UI.Components.Windows.MinorWindows.GraphViewer"
                         xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
                         xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
                         xmlns:styleClasses = "clr-namespace:Arcanum.UI.Components.StyleClasses"
                         mc:Ignorable = "d"
                         Title = "GraphViewer"
                         Height = "700"
                         Width = "1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width = "1*" />
            <ColumnDefinition Width = "1*" />
            <ColumnDefinition Width = "4*" />
        </Grid.ColumnDefinitions>

        <ListView Grid.Column = "0"
                  ItemsSource = "{Binding Eu5Objects, RelativeSource= {RelativeSource AncestorType= styleClasses:BaseWindow}}"
                  SelectionChanged = "Selector_OnSelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text = "{Binding Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ListView Grid.Column = "1"
                  ItemsSource = "{Binding ObjectsOfType, RelativeSource= {RelativeSource AncestorType= styleClasses:BaseWindow}}"
                  SelectionChanged = "ObjectSelector_OnSelectionChanged"
                  x:Name = "ObjectListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text = "{Binding UniqueId}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Canvas Grid.Column = "2"
                x:Name = "GraphCanvas">
        </Canvas>

    </Grid>
</styleClasses:BaseWindow>