<Window x:Class = "Arcanum.UI.Components.Windows.MinorWindows.PopUpShell"
        xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
        xmlns:styleClasses = "clr-namespace:Arcanum.UI.Components.StyleClasses"
        mc:Ignorable = "d"
        Title = "PopUpShell"
        Height = "450"
        Width = "800"
        AllowsTransparency = "True"
        Background = "Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source = "/Arcanum_UI;component/Assets/ArcanumShared/DefaultPalette.xaml" />
                <ResourceDictionary Source = "/Arcanum_UI;component/Assets/ArcanumShared/DefaultFonts.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border BorderBrush = "{StaticResource DefaultBorderColorBrush}"
            BorderThickness = "1"
            Background = "{StaticResource DefaultBackColorBrush}"
            CornerRadius = "5"
            Margin = "0">
        <Border.Effect>
            <DropShadowEffect ShadowDepth = "2"
                              Opacity = "0.4"
                              BlurRadius = "15"
                              Direction = "270" />
        </Border.Effect>

        <Grid Margin = "5">
            <Grid.RowDefinitions>
                <RowDefinition Height = "Auto" /> <!-- Title -->
                <RowDefinition Height = "*" />    <!-- Dynamic Content -->
                <RowDefinition Height = "Auto" /> <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock x:Name = "TitleTextBlock"
                       Grid.Row = "0"
                       Text = "{Binding DialogTitle, RelativeSource={RelativeSource AncestorType=Window}}"
                       FontSize = "16"
                       HorizontalAlignment = "Center"
                       VerticalAlignment = "Center"
                       FontWeight = "Bold"
                       Margin = "0,0,0,5" />

            <!-- This is where your custom control (e.g., ColorPicker) will go -->
            <ContentPresenter x:Name = "ContentHost"
                              Grid.Row = "1"
                              Content = "{Binding HostedContent, RelativeSource={RelativeSource AncestorType=Window}}" />

            <!-- Standard Buttons -->
            <StackPanel Grid.Row = "2"
                        Orientation = "Horizontal"
                        HorizontalAlignment = "Right"
                        Margin = "0,5,0,0">
                <styleClasses:BaseButton Content = "Confirm"
                                         Width = "80"
                                         Margin = "0,0,10,0"
                                         IsDefault = "True"
                                         Click = "Confirm_Click" />
                <styleClasses:BaseButton Content = "Cancel"
                                         Width = "80"
                                         IsCancel = "True"
                                         Click = "Cancel_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>