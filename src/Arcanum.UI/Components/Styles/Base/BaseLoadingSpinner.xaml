<ResourceDictionary xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source = "/Arcanum_UI;component/Assets/ArcanumShared/DefaultPalette.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key = "BaseLoadingSpinner" TargetType = "ContentControl">
        <Setter Property = "Margin" Value = "0" />
        <Setter Property = "Template">
            <Setter.Value>
                <ControlTemplate TargetType = "ContentControl">
                    <AdornerDecorator>
                        <Grid>
                            <ContentPresenter />

                            <Border x:Name = "Overlay" Background = "{StaticResource DefaultBackColorBrush}"
                                    Visibility = "Collapsed" HorizontalAlignment = "Stretch"
                                    VerticalAlignment = "Stretch">
                                <Viewbox Width = "40" Height = "40">
                                    <Path x:Name = "LoadingSpinner" Stroke = "{StaticResource BlueAccentColorBrush}"
                                          StrokeThickness = "5" HorizontalAlignment = "Center"
                                          Margin = "{TemplateBinding Margin}" VerticalAlignment = "Center"
                                          RenderTransformOrigin = "0,0">
                                        <Path.Data>
                                            <PathGeometry>
                                                <!-- Start the arc at the top-center point -->
                                                <PathFigure StartPoint = "22.5,22.5">
                                                    <!-- The ArcSegment defines the curve -->
                                                    <ArcSegment Point = "42.5,2.5" Size = "20,20" IsLargeArc = "True"
                                                                SweepDirection = "Clockwise" />
                                                </PathFigure>
                                            </PathGeometry>
                                        </Path.Data>

                                        <!-- This transform will be animated to create the rotation -->
                                        <Path.RenderTransform>
                                            <RotateTransform x:Name = "Rotation" Angle = "0" CenterX = "22.5"
                                                             CenterY = "2.5" />
                                        </Path.RenderTransform>

                                    </Path>
                                </Viewbox>
                            </Border>
                        </Grid>
                    </AdornerDecorator>

                    <ControlTemplate.Triggers>
                        <DataTrigger Binding = "{Binding Tag, RelativeSource={RelativeSource Self}}" Value = "False">
                            <Setter TargetName = "Overlay" Property = "Visibility" Value = "Visible" />
                            <DataTrigger.EnterActions>
                                <BeginStoryboard x:Name = "SpinAction">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName = "Rotation"
                                                         Storyboard.TargetProperty = "Angle" From = "0" To = "360"
                                                         Duration = "0:0:1" RepeatBehavior = "Forever" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <StopStoryboard BeginStoryboardName = "SpinAction" />

                                <BeginStoryboard>
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName = "Overlay"
                                                                       Storyboard.TargetProperty = "Visibility">
                                            <DiscreteObjectKeyFrame KeyTime = "0"
                                                                    Value = "{x:Static Visibility.Collapsed}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>