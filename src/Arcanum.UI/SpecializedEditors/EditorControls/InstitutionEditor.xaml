<UserControl x:Class = "Arcanum.UI.SpecializedEditors.EditorControls.InstitutionEditor"
             xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels = "clr-namespace:Arcanum.UI.SpecializedEditors.EditorControls.ViewModels"
             xmlns:specializedEditors = "clr-namespace:Arcanum.UI.Components.UserControls.SpecializedEditors"
             mc:Ignorable = "d" d:DesignHeight = "300" d:DesignWidth = "300"
             d:DataContext = "{d:DesignInstance viewModels:InstitutionViewModel}">

    <StackPanel>
        <!-- Title: Institutions for {Location}-->
        <TextBlock FontSize = "16" FontWeight = "Bold" Margin = "0,0,0,10" Text = "{Binding HeaderText}"
                   HorizontalAlignment = "Center" />

        <!-- Institution Setters bound to 'InstitutionSetters' -->
        <ItemsControl ItemsSource = "{Binding InstitutionSetters}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <specializedEditors:InstitutionSetter DataContext = "{Binding}" Margin = "0,0,0,2" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>