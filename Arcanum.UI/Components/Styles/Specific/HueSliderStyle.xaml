<ResourceDictionary xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source = "/Arcanum_UI;component/Assets/ArcanumShared/DefaultPalette.xaml" />
        <ResourceDictionary Source = "/Arcanum_UI;component/Assets/ArcanumShared/DefaultFonts.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key = "TransparentSliderRepeatButtonStyle"
           TargetType = "RepeatButton">
        <Setter Property = "IsHitTestVisible"
                Value = "False">
        </Setter>
        <Setter Property = "Template">
            <Setter.Value>
                <ControlTemplate TargetType = "RepeatButton">
                    <Rectangle Fill = "Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key = "WideSliderThumbStyle"
           TargetType = "{x:Type Thumb}">
        <Setter Property = "Width"
                Value = "32" />
        <Setter Property = "Height"
                Value = "7" />
        <Setter Property = "Template">
            <Setter.Value>
                <ControlTemplate TargetType = "{x:Type Thumb}">
                    <Grid Background = "Transparent"
                          IsHitTestVisible = "True"
                          SnapsToDevicePixels = "True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width = "10" />
                            <ColumnDefinition Width = "*" />
                            <ColumnDefinition Width = "10" />
                        </Grid.ColumnDefinitions>

                        <Path Grid.Column = "0"
                              Fill = "White"
                              Stroke = "Black"
                              StrokeThickness = "1"
                              Stretch = "Fill"
                              Data = "M0,0 L0,2 L1,1 Z"
                              SnapsToDevicePixels = "True" />

                        <Path Grid.Column = "2"
                              Fill = "White"
                              Stroke = "Black"
                              StrokeThickness = "1"
                              Stretch = "Fill"
                              Data = "M1,0 L1,2 L0,1 Z"
                              SnapsToDevicePixels = "True" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key = "SliderWithoutTrackBarStyle"
           TargetType = "{x:Type Slider}">
        <Setter Property = "Width"
                Value = "32">
        </Setter>
        <Setter Property = "Template">
            <Setter.Value>
                <ControlTemplate TargetType = "{x:Type Slider}">
                    <Grid>
                        <Border Background = "{TemplateBinding Background}"
                                SnapsToDevicePixels = "True"
                                Margin = "4,0">
                        </Border>

                        <Track x:Name = "PART_Track">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command = "{x:Static Slider.DecreaseLarge}"
                                              Style = "{StaticResource TransparentSliderRepeatButtonStyle}" />
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command = "{x:Static Slider.IncreaseLarge}"
                                              Style = "{StaticResource TransparentSliderRepeatButtonStyle}" />
                            </Track.IncreaseRepeatButton>

                            <Track.Thumb>
                                <Thumb Style = "{StaticResource WideSliderThumbStyle}"
                                       HorizontalAlignment = "Center"
                                       VerticalAlignment = "Center" />
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>