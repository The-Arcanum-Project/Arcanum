<ResourceDictionary xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dig = "clr-namespace:Arcanum.Core.CoreSystems.ErrorSystem.Diagnostics;assembly=Arcanum.Core">

    <DataTemplate DataType = "{x:Type dig:Diagnostic}">

        <Grid Height = "18"
              Margin = "1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width = "24" />
                <ColumnDefinition Width = "250" />
                <ColumnDefinition Width = "*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column = "0"
                    x:Name = "SeverityBorder"
                    HorizontalAlignment="Left"
                    CornerRadius = "4"
                    Margin="0, 1">
                <Border.Style>
                    <Style TargetType = "Border">
                        <Setter Property = "Background"
                                Value = "Transparent" />
                        <Style.Triggers>
                            <DataTrigger Binding = "{Binding Severity}"
                                         Value = "Error">
                                <Setter Property = "Background"
                                        Value = "{StaticResource DimErrorRedColorBrush}" />
                            </DataTrigger>
                            <DataTrigger Binding = "{Binding Severity}"
                                         Value = "Warning">
                                <Setter Property = "Background"
                                        Value = "{StaticResource DimWarningYellowColorBrush}" />
                            </DataTrigger>
                            <DataTrigger Binding = "{Binding Severity}"
                                         Value = "Information">
                                <Setter Property = "Background"
                                        Value = "{StaticResource DimInformationBlueColorBrush}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Image VerticalAlignment = "Center"
                       HorizontalAlignment = "Center"
                       Width = "16"
                       Height = "16">
                    <Image.Style>
                        <Style TargetType = "Image">
                            <Setter Property = "Source"
                                    Value = "pack://application:,,,/Assets/Icons/16x16/InfoCircle16x16.png" />
                            <Style.Triggers>
                                <DataTrigger Binding = "{Binding Severity}"
                                             Value = "Error">
                                    <Setter Property = "Source"
                                            Value = "pack://application:,,,/Assets/Icons/16x16/CloseCircle16x16.png" />
                                </DataTrigger>
                                <DataTrigger Binding = "{Binding Severity}"
                                             Value = "Warning">
                                    <Setter Property = "Source"
                                            Value = "pack://application:,,,/Assets/Icons/16x16/DangerCircle16x16.png" />
                                </DataTrigger>
                                <DataTrigger Binding = "{Binding Severity}"
                                             Value = "Information">
                                    <Setter Property = "Source"
                                            Value = "pack://application:,,,/Assets/Icons/16x16/InfoCircle16x16.png" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
            </Border>


            <TextBlock Grid.Column = "1"
                       Text = "{Binding Descriptor }">
            </TextBlock>

            <TextBlock Grid.Column = "2"
                       Text = "{Binding Descriptor.Message }">
            </TextBlock>
        </Grid>

    </DataTemplate>


</ResourceDictionary>