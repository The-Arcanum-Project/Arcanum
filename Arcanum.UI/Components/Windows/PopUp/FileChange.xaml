<styleClasses:BaseWindow x:Class="Arcanum.UI.Components.Windows.PopUp.FileChange"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                         xmlns:styleClasses="clr-namespace:Arcanum.UI.Components.StyleClasses"
                         xmlns:popUp="clr-namespace:Arcanum.UI.Components.Windows.PopUp"
                         mc:Ignorable="d"
                         Title="File Changed"
                         Height="200"
                         Width="400"
                         SizeToContent="WidthAndHeight"
                         FooterHeight="0"
                         ShowInTaskbar="False"
                         ShowMaximizeButton="False"
                         ShowMinimizeButton="False"
                         WindowBorderThickness="2"
                         BorderBrush="{StaticResource DefaultBorderColorBrush}"
                         WindowCornerRadius="2"
                         AllowsTransparency="True"
                         Background="{StaticResource DefaultBackColorBrush}">

   <Window.Resources>
      <ResourceDictionary>
         <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="/Arcanum_UI;component/Assets/ArcanumShared/DefaultPalette.xaml" />
            <ResourceDictionary Source="/Arcanum_UI;component/Components/Styles/Base/BaseButton.xaml" />
         </ResourceDictionary.MergedDictionaries>
      </ResourceDictionary>
   </Window.Resources>

   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="1*" />
         <RowDefinition Height="2*"/>
         <RowDefinition Height="1*" />
      </Grid.RowDefinitions>
      <TextBlock Grid.Row="0"
                   Text="The following files have been changed externally. Choose options to handle the changes."
                   TextWrapping="Wrap" Grid.RowSpan="2" />
      <ListView Grid.Row="1"
                x:Name="ResultView"
                ItemsSource="{Binding SearchResult, RelativeSource={RelativeSource AncestorType=popUp:FileChange} }"
                MaxHeight="200"
                BorderThickness="1"
                SelectionMode="Single"
                MinWidth="{Binding ActualWidth, ElementName=SearchBox}">
         <ListView.ItemTemplate>
            <DataTemplate>
               <TextBlock Text="{Binding Name}"/>
            </DataTemplate>
         </ListView.ItemTemplate>
      </ListView>
      <StackPanel Grid.Row="2">

      </StackPanel>
   </Grid>
</styleClasses:BaseWindow>