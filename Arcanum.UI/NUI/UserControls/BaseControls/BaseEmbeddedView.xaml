<UserControl x:Class = "Arcanum.UI.NUI.UserControls.BaseControls.BaseEmbeddedView"
             xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local = "clr-namespace:Arcanum.UI.NUI.UserControls.BaseControls"
             mc:Ignorable = "d"
             d:DesignHeight = "300"
             d:DesignWidth = "300">

    <Grid x:Name = "ContentGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width = "*" />
        </Grid.ColumnDefinitions>

        <Border x:Name = "EmbedMarker"
                Grid.Column = "0"
                Background = "{StaticResource SelectedBackColorBrush}"
                BorderBrush = "{StaticResource SelectedBackColorBrush}"
                BorderThickness = "2"
                CornerRadius = "1"
                Width = "4"
                Margin = "0, 0, 2, 0"
                HorizontalAlignment = "Left">
        </Border>
    </Grid>

</UserControl>